model User {
    id        String   @id @default(uuid())
    name      String
    photo     String?
    email     String   @unique
    password  String
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt

    @@index([email])
    @@map("users")
}
